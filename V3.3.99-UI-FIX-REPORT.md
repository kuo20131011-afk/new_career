# V3.3.99 UI 修正報告

- 履歷「查看詳細內容」改為浮動視窗，顯示目前上傳履歷的完整擷取文字；Esc、點背景、右上角 X 可關閉。
- 履歷主頁只保留短摘要，避免整份履歷把頁面拉長。
- 「一鍵產生」執行時會顯示旋轉指示、目前階段（第 1/3、2/3、3/3）與即時狀態，不再像沒有執行。
- 五步驟導覽移除舊版 pseudo-element 線條，改用單一實體線，確保不會出現雙條線。
- 增加主要區塊之間的安全留白。
- 保留既有 API、Provider、功能 ID 與 Google 登入隱藏測試模式。
