# 職透 JobSight V3.3.87 — AI API 供應商緊急修復

## 本版重點
- Agnes AI 預設模型改為 `agnes-2.5-flash`；可直接輸入 `agnes-3.0-flash` 或任何原廠新 Model ID。
- API Key 設定新增 Base URL 與 Model ID，模型不再由程式鎖死。
- 新增 NVIDIA NIM、GroqCloud、OpenRouter、Mistral AI Studio 四個供應商。
- OpenAI 相容供應商共用同一套 Chat Completions adapter，減少日後原廠變更造成的重複維護。
- 支援「讀取模型」：若原廠允許瀏覽器 GET /models 且 CORS/權限允許，會載入目前帳戶可用模型；失敗時仍可手動輸入。
- 所有 Key 與 Base URL / Model 設定只存在 sessionStorage，不上傳到 Netlify 後端。

## 目前預設值（皆可修改）
- Claude: `claude-sonnet-5`
- Gemini: `gemini-3.8-flash`
- OpenAI: `gpt-5.6-luna`
- Agnes AI: `agnes-2.5-flash`
- NVIDIA NIM: `openai/gpt-oss-20b`
- GroqCloud: `openai/gpt-oss-20b`
- OpenRouter: `openrouter/free`
- Mistral AI Studio: `mistral-small-latest`

注意：預設值不是「免費模型鎖定」。使用者可直接修改 Model ID；免費模型是否仍可用、額度與限制由各原廠帳戶與當時方案決定。

## 驗證
- `node --check js/api-keys.js`：通過
- `node --check js/ai-providers.js`：通過
- NVIDIA NIM OpenAI-compatible adapter mock integration test：通過
- 八家供應商 UI 欄位與 dispatch 靜態檢查：通過
