# V3.3.100 UI FIX

- Header：隱藏重複的頂部功能導覽，保留品牌與 API 設定，避免文字重疊。
- STEP 01–05：停用舊 `.tab-btn:not(:last-child)::before` 與 `.tabs::before`，只保留 `.v399-step-line` 單一連線。
- 一鍵產生：執行中按鈕顯示目前階段，右側狀態列固定顯示執行中、已花時間、預估剩餘時間與 tokens。
- 每一階段完成後顯示「已完成，準備下一階段」。
