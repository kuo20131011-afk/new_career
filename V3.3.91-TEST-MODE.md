# V3.3.91 測試模式

本版暫時隱藏 Google／Netlify Identity 登入牆，開啟網站後直接進入主工具。

## 開關

檔案：`js/auth.js`

```js
const TEMP_BYPASS_LOGIN_FOR_TESTING = true;
```

測試完成後改成 `false` 即可恢復正式登入牆。

此版本沒有刪除 Netlify Identity，也沒有移除後端身份驗證。
