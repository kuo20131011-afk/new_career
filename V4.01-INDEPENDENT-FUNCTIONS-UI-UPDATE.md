# V4.01 — 五大功能獨立產生 UI 更新

## 本次調整
- 移除「✨ 一鍵產生履歷分析（媒合／健診／模擬面試）」總按鈕與其操作入口。
- 五項功能改為各自獨立執行：履歷媒合、履歷健診、模擬面試、客製履歷、提案簡報。
- 五張功能卡均提供「✨ 立即生成」入口。
- 點擊功能卡其他區域：只切換到該功能，不會誤觸 AI 生成。
- 點擊「立即生成」：切換到該功能並直接執行原本對應的 AI 產生函式。
- 保留既有各功能的生成邏輯、結果、tokens/時間統計與併發鎖。
- 新增清楚的操作說明：五項功能可任選，也可以依 01 → 05 逐步完成。
- 客製履歷與提案簡報的說明文字改為獨立功能提示，不再引用已移除的一鍵產生流程。

## 驗證
- Node.js `--check` 已通過：ai-providers.js、tabs-core.js、tab-match.js、tab-healthcheck.js、tab-interview.js、tab-tailored-resume.js、tab-deck.js。
- 舊版總按鈕 DOM 已從 index.html 移除。
- AI 按鈕鎖定清單已移除 runAllBtn，避免依賴不存在的 DOM。
